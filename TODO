ok* save score as # of moves per set>level (one file/set in ~/.rsokoban/record)
ok* Tk: display "new record" dialog when score is beaten
ok* display record for current level in screen-header
ok* create ~/.rsokoban/record at program starting if it doesn't exist


* skinning
  + 2/3 skins shipped with the software
  + deployment of new skins in ~/.rsokoban/skins
	+ menu to load a skin that lists all available skins (shipped ones + added)
  + skin as multiple files
	+ skin as one big file
	+ support for multiple wall images
  + several size of tiles or resizing ?

* Allow user to specify the max size of the map on screen in cells (default 19x16)

* Give a try to Tk::Tile

-------------------------------------------------------
Think about:
* gnome GUI (gtk only to be windows compatible ?)
* give a try to rubygame ?
* scoring moves and pushes ?
* level preview ?
* solver (plugin ?)
* zoom ?
* format fichier niveau xml (.slc)
* other GUI(s) ?
* mouse support
* i18n
